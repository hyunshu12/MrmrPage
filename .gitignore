# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
.idea

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*
!.env.local.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# private runbooks
docs/private/*.md
docs/private/**/*.md

# AI tooling / session artifacts (do not track)
# bkit plugin runtime: audit logs, snapshots, checkpoints, PDCA/session state
/.bkit/
# playwright-mcp dumps: console logs, page snapshots (.yml) and screenshots (.png)
/.playwright-mcp/
# local Claude Code config & worktree metadata (keep settings.json shareable via negation if desired)
.claude/worktrees/
