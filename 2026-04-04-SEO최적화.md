# mrmr.kr SEO 최적화 계획

> 작성일: 2026-04-04  
> 목표: "디미고 무럭무럭", "무럭무럭", "디미고 무럭무럭 홈페이지" 구글 상위 노출

---

## 현황 진단

**잘 구현된 것**: metadataBase, robots, canonical, sitemap, JSON-LD 기초, preload/preconnect

**반드시 수정 필요**:
- `.env.local`에 Google/Naver 인증 코드 값이 없어 search console 인증 불가
- OG 이미지 1200x1200 (summary_large_image는 1200x630 필요)
- JSON-LD Organization 타입이 동아리에 맞지 않음 (ClubOrSportsTeam 권장)
- FAQ 페이지가 `'use client'`라 FAQ 텍스트가 SSR 안 됨 → 리치 스니펫 획득 불가
- robots.ts에 `/api/` disallow 없음

---

## 키워드 전략

| 키워드 | 경쟁 강도 | 목표 |
|--------|----------|------|
| 디미고 무럭무럭 | 낮음 | 1위 |
| 무럭무럭 동아리 | 낮음 | 1위 |
| 디미고 스마트팜 | 낮음 | 1위 |
| 디미고 동아리 | 중간 | 상위 5위 |
| 디미고 | 매우 높음 | **목표 제외** (학교 공식 사이트와 경쟁 불가) |

---

## P0 — 즉시 적용 (인프라)

### 1. Google/Naver Search Console 인증 코드 입력

`.env.local`에 추가:
```
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=<Google HTML 태그 content 값>
NEXT_PUBLIC_NAVER_SITE_VERIFICATION=<네이버 메타태그 content 값>
```

- **Google**: https://search.google.com/search-console → URL 접두어 → HTML 태그 방식
- **Naver**: https://searchadvisor.naver.com → 사이트 등록 → 메타태그 방식
- 배포 후 양쪽에서 sitemap.xml 제출: `https://www.mrmr.kr/sitemap.xml`

### 2. OG 이미지 규격 수정 — `src/app/layout.tsx`

- `/public/og-image.png` 제작 (1200x630, 로고 + "디미고 무럭무럭 스마트팜 동아리" 텍스트)
- metadata의 OG 이미지를 `/og-image.png`, `width: 1200`, `height: 630`으로 변경

---

## P1 — 이번 주 내 적용 (SEO 신호 품질)

### 3. JSON-LD 개선 — `src/app/layout.tsx`

```typescript
// Organization → ClubOrSportsTeam + memberOf(디미고) + sameAs(소셜)
organizationJsonLd['@type'] = 'ClubOrSportsTeam'
organizationJsonLd.memberOf = {
  '@type': 'EducationalOrganization',
  name: '한국디지털미디어고등학교',
  alternateName: '디미고',
  url: 'https://www.dimigo.hs.kr',
}
organizationJsonLd.sameAs = [ /* 인스타그램, 깃헙 등 실제 운영 소셜 URL */ ]

// WebSite에 SearchAction 추가 (구글 사이트링크 검색창)
websiteJsonLd.potentialAction = {
  '@type': 'SearchAction',
  target: { '@type': 'EntryPoint', urlTemplate: `${SITE_URL}/projects?q={search_term_string}` },
  'query-input': 'required name=search_term_string',
}
```

### 4. FAQ 페이지 SSR 전환 + FAQPage JSON-LD — `src/app/faq/`

현재 `faq/page.tsx`가 `'use client'`라 FAQ 내용이 구글에 인덱싱 안 됨. 분리 전략:
- `faq/page.tsx` → Server Component (FAQ 데이터 + FAQPage JSON-LD 포함)
- `faq/FaqAccordion.tsx` → Client Component (신규 생성, useState 아코디언만)

FAQPage JSON-LD 추가 (구글 검색 결과에서 리치 스니펫으로 표시 → CTR 대폭 향상):
```typescript
const faqJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map(faq => ({
    '@type': 'Question',
    name: faq.question,
    acceptedAnswer: { '@type': 'Answer', text: faq.answer },
  })),
}
```

### 5. robots.ts에 `/api/` disallow 추가 — `src/app/robots.ts`

```typescript
disallow: ['/api/'],
```

### 6. sitemap.ts lastModified 실제 날짜로 수정 — `src/app/sitemap.ts`

매 빌드마다 `new Date()`로 고정되는 문제 → 실제 마지막 콘텐츠 수정일로 하드코딩

---

## P2 — 이번 달 내 적용 (콘텐츠 SEO)

### 7. 홈 페이지 metadata 추가 — `src/app/page.tsx`

```typescript
export const metadata: Metadata = {
  title: '무럭무럭 | 디미고 스마트팜 동아리',
  description: '한국디지털미디어고등학교(디미고) 유일 스마트팜 동아리 무럭무럭 공식 웹사이트.',
}
```

### 8. 각 페이지별 OG 이미지 추가 — 각 layout.tsx

`/public/` 에 추가할 파일:
- `og-image.png` (홈, 1200x630)
- `og-projects.png`, `og-achievements.png`, `og-members.png`, `og-faq.png`

### 9. BreadcrumbList JSON-LD 각 페이지 layout에 추가

```typescript
const breadcrumbJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: '홈', item: 'https://www.mrmr.kr' },
    { '@type': 'ListItem', position: 2, name: '프로젝트', item: 'https://www.mrmr.kr/projects' },
  ],
}
```

### 10. Footer 소셜 링크 추가 — `src/components/layout/Footer.tsx`

Instagram, GitHub 등 운영 중인 채널 링크 추가 → sameAs 신뢰도 연결

---

## P3 — 장기 전략 (콘텐츠 확충)

- FAQ 내용 확충: "디미고 동아리 지원", "스마트팜 동아리 활동" 등 롱테일 키워드 문답 추가
- Notion의 프로젝트/업적 설명 텍스트 충실하게 작성 (SEO의 핵심 자산)
- 4주 후 구글 서치 콘솔 "검색어" 탭에서 실제 노출 키워드 확인 후 전략 조정

---

## 주요 수정 파일 목록

| 파일 | 작업 내용 |
|------|----------|
| `src/app/layout.tsx` | OG 이미지 규격, JSON-LD 개선 |
| `src/app/faq/page.tsx` | SSR 전환 + FAQPage JSON-LD (임팩트 가장 큼) |
| `src/app/faq/FaqAccordion.tsx` | 신규 생성 (클라이언트 아코디언 분리) |
| `src/app/robots.ts` | /api/ disallow 추가 |
| `src/app/sitemap.ts` | lastModified 실제 날짜로 수정 |
| `src/app/page.tsx` | 홈 페이지 metadata 추가 |
| `src/components/layout/Footer.tsx` | 소셜 링크 추가 |
| `.env.local` | 인증 코드 추가 (직접 진행) |
| `public/og-image.png` | 1200x630 OG 이미지 제작 (직접 진행) |

---

## 검증 방법

1. `https://www.mrmr.kr/robots.txt` — robots 설정 확인
2. `https://www.mrmr.kr/sitemap.xml` — 5개 URL 확인
3. [Google Rich Results Test](https://search.google.com/test/rich-results) — FAQ 리치 스니펫 테스트
4. [Open Graph Debugger](https://developers.facebook.com/tools/debug/) — OG 이미지 비율 확인
5. 구글 서치 콘솔 → 인증 완료 후 sitemap 제출
6. 2~4주 후 "검색어" 탭에서 "디미고 무럭무럭" 노출 확인
